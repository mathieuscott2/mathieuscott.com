import{hasAccess}from"../../rafa-shared-agenda/auth";import{getAgenda,manageOption,updateDays}from"../../lib/rafaDb";
export async function GET(r:Request){if(!await hasAccess())return Response.json({error:"Unauthorized"},{status:401});const u=new URL(r.url),from=u.searchParams.get("from")??"2026-08-18",to=u.searchParams.get("to")??"2026-09-21";return Response.json(await getAgenda(from,to))}
export async function PATCH(r:Request){if(!await hasAccess())return Response.json({error:"Unauthorized"},{status:401});const b=await r.json()as{dates:string[],field:string,value:string};if(!b.dates?.length||!b.value)return Response.json({error:"Invalid"},{status:400});await updateDays(b.dates,b.field,b.value);return Response.json({ok:true})}
export async function POST(r:Request){if(!await hasAccess())return Response.json({error:"Unauthorized"},{status:401});const b=await r.json()as{type:"person"|"location",action:"add"|"rename"|"remove",name:string,oldName?:string};try{await manageOption(b.type,b.action,b.name,b.oldName);return Response.json({ok:true})}catch(e){return Response.json({error:e instanceof Error?e.message:"Could not update option"},{status:400})}}
